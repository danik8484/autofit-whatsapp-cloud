[phases.setup]
nixPkgs = ["nodejs_20", "python311", "python311Packages.pip"]

[phases.install]
cmds = ["npm install", "pip3 install -r requirements.txt --break-system-packages"]

[start]
cmd = "node index.js"
