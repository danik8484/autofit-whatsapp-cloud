[build]
builder = "nixpacks"
buildCommand = "npm install && pip3 install -r requirements.txt && playwright install chromium"

[deploy]
startCommand = "node index.js"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 5
